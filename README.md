## 项目说明

本人为初学者，并且仅作为爱好进行开发，代码质量不高，且相当一部分是由AI生成的(ws2812的驱动完全由AI生成)，

希望初学者可以和我共同进步，希望大佬可以提出宝贵的意见

## 版本说明
#### v0.2

更改了按键逻辑，现在使用三个按键，一个用来切换模式，同时长按进入调色模式，另外两个按键负责选中通道的亮度加减

模式切换按键改为了使用了中断加标志位的方式进行消抖以及状态的传递

删除了keys.c和keys.h

灯效部分需要修改，目前部分灯效函数阻塞严重，将来要改成主循环调用，effects函数只负责一帧的形式

此外，这次还配置了i2c用于驱动oled屏幕显示状态和颜色值，暂时未开发

#### v0.1

这个项目最初是把所有逻辑都写在main.c里的，该版本为重构后的第一版，将特效和按键检测的部分逻辑分别分离到了 effects.c 和 key.c
